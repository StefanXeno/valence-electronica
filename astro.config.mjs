// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // TODO: replace OWNER with the GitHub username/org once the GitHub repo exists.
  site: 'https://OWNER.github.io',
  base: '/valence-electronica',
});
